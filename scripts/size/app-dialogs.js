// Dialogs without toasts. The package sets `sideEffects: false`, so this measures what a
// caller who only opens dialogs actually pays.
export { createDialogsController, createDialogTheme } from "../../dist/index.js";
